import {StyleSheet} from 'react-native';

const TodoFilterStyle = StyleSheet.create({
  filterWrapper: {
    display: 'flex',
    flexDirection: 'row',
    paddingHorizontal: 10,
    backgroundColor: '#FFF',
    paddingBottom: 8,
    alignItems: 'center',
  },
});

export default TodoFilterStyle;
